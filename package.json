{
  "name": "node-logger-middleware",
  "version": "1.4.0",
  "description": "node-logger-middleware used to log the request url, method and time and then saves it in server in log.txt",
  "main": "index.js",
  "keywords": ["node-log-middleware","log-information","node-logger","logger-middleware","npm-logger"],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mohammed Nafih Yuseph",
  "license": "ISC",
  "dependencies": {
    "date-fns": "^2.30.0"
  }
}
